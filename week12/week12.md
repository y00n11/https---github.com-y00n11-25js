const pet{
    nsme:'구름'
    eat:function(food)   
        >function 제거해도 됨(이럴 경우 'eat(food)'로 변경)
        >> eat(food)
}

진도 {
    교재261~266 생략(우선은..)
    프로퍼티도 중요한데 시간 부족으로...
}

---
만약 1-6 사이에 숫자가 나오게 하는 코드

const num=Math.random()
        // 0<=   <1
        // 무조건 +1
        // 1<=   <2

    if) 6씩 곱한다면
    1~6사이는 숫자 6개가 필요함
    >>즉, 6을 곱해주면 됨

const num=Math.floor(Math.random()*6+1)
>>가짓수만큼 곱하고 플로우를 더한다? 
    >> 이 코드 안에서 6=가짓수, 1=시작값
        >> 가짓수는 시작값-마지막값+1= 가짓수 
            >> (ex' 2~7이면 2-7+1=6) 
                >> +1 이유: 시작값 자신도 포함해야하니까

--
최종!!!!!!
// ❌ 틀린 이해
// "7까지" = 7을 곱한다 (X)
// "70까지" = 70을 곱한다 (X)

// ✅ 올바른 이해
// 1~7 = 7개 필요 → *7 + 1
// 2~7 = 6개 필요 → *6 + 2
// 1~70 = 70개 필요 → *70 + 1
